---
- hosts: all
  become: true

  tasks:
    - name: install httpd on servers
      yum: name=httpd update_cache=yes state=latest
      notify:
       - Start http server
        service:
        name=httpd
        state=restarted
        ---
   
